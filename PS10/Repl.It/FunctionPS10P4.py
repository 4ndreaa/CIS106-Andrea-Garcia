def total_and_averageP4(s1, s2, s3):
  # Compute total points
  total_points = s1 + s2 + s3

  # Compute average exam score
  average_score = total_points / 3

  # Return total points and average score
  return total_points, average_score